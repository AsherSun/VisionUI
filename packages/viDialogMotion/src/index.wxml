<view wx:if="{{ishide}}" class="vi-dialog">
  <view class="vi-dialog-mask" data-sign="mask" capture-catch:tap="triggerToHide"></view>
  <view class="vi-dialog-content animated {{animateName}}">
    <slot></slot>
  </view>
</view>

