<view class="cell-group_wrap" style="margin-top: {{marginTop}}px">
  <view class="cell-group__title" wx:if="{{title}}">{{title}}</view>
  <view class="cell-group__container">
    <slot></slot>
  </view>
</view>